// rustimport:py3

use pyo3::prelude::*;

#[pyfunction]
fn eulers_method(y_n: i64, t_final: i64, h: u64) -> (Vec<i64>, Vec<i64>) {
    let y_n = vec![y_n]
    let t_n vec![0]
    while (t_n[-1] < t_final) {
        
    }
}