#ifndef CALCULATOR_H
#define CALCULATOR_H

typedef enum
{
    ADDITION = '+',
    SUBTRACTION = '-',
    DIVISION = '/',
    // INT_DIVISION = '//',
    MULTIPLICATION = '*',
    EXPONENTIATION = '^',
    // ALT_EXPONENTIATION = '**',
    MODULUS = '%',
    LEFT_PAREN = '(',
    RIGHT_PAREN = ')',
} operators;

#endif // CALCULATOR_H